# Tests Directory

This folder will hold unit and smoke tests for reusable code and the API.

Good first tests:

- feature list consistency
- price-context category assignment
- uncertainty interval application
- FastAPI `/health` and `/predict` smoke tests

Run from the repository root:

```powershell
python -m pytest
```
